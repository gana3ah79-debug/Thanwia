name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Inject navigation controls
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('android_project/app/src/main/assets/www/index.html')
          s = p.read_text(encoding='utf-8')
          tag = '<script src="navigation.js"></script>'
          if tag not in s:
              s = s.replace('</body>', tag + '\n</body>', 1)
          p.write_text(s, encoding='utf-8')
          if tag not in s:
              raise SystemExit('navigation.js injection failed')
          print('Navigation controls injected')
          PY

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle 8.10
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10'

      - name: Build debug APK
        working-directory: android_project
        run: gradle --no-daemon :app:assembleDebug

      - name: Verify navigation controls inside APK
        shell: bash
        run: |
          unzip -p android_project/app/build/outputs/apk/debug/app-debug.apk assets/www/index.html > /tmp/index.html
          grep -F 'navigation.js' /tmp/index.html
          unzip -l android_project/app/build/outputs/apk/debug/app-debug.apk | grep 'assets/www/navigation.js'
          echo 'Navigation controls verified in APK'

      - name: Verify APK exists
        run: test -f android_project/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Thanwia-APK
          path: android_project/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
