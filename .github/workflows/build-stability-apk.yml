name: Build Thanwia Clean Core APK
on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify clean navigation core
        run: |
          grep -q 'rihlaCoreV8' android_project/app/src/main/assets/www/app-core-v8.js
          grep -q 'window.show=function(id)' android_project/app/src/main/assets/www/app-core-v8.js
          grep -q 'rihlaLogoutV8' android_project/app/src/main/assets/www/app-core-v8.js
          grep -q 'app-core-v8.js' android_project/app/src/main/java/com/rihla/thanaweya/MainActivity.java
          ! grep -q 'rihlaNavigationLogoutV6' android_project/app/src/main/java/com/rihla/thanaweya/MainActivity.java
          ! grep -q 'rihlaNavFixV5' android_project/app/src/main/java/com/rihla/thanaweya/MainActivity.java
          ! grep -q 'rihlaStabilityV4' android_project/app/src/main/java/com/rihla/thanaweya/MainActivity.java
          ! grep -q 'app-core-v7.js' android_project/app/src/main/java/com/rihla/thanaweya/MainActivity.java
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10'
      - name: Build APK
        run: gradle --no-daemon :app:assembleDebug
        working-directory: android_project
      - name: Verify APK
        run: |
          APK=android_project/app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          unzip -p "$APK" assets/www/app-core-v8.js | grep -q 'rihlaLogoutV8'
          unzip -p "$APK" assets/www/app-core-v8.js | grep -q 'window.show=function(id)'
          unzip -p "$APK" assets/www/app-core-v8.js | grep -q 'storage:window.localStorage'
      - uses: actions/upload-artifact@v4
        with:
          name: Thanwia-Clean-Core-V8-APK
          path: android_project/app/build/outputs/apk/debug/app-debug.apk
